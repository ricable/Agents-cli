# Command Reference — cli-anything-kdenlive

## project

### `project-list`

list project

**Arguments:**
  - `--id` (string) — project identifier

### `project-create`

create project

**Arguments:**
  - `--id` (string) — project identifier

### `project-get`

get project

**Arguments:**
  - `id` (string, required) — project identifier

### `project-update`

update project

**Arguments:**
  - `id` (string, required) — project identifier

### `project-delete`

delete project

**Arguments:**
  - `id` (string, required) — project identifier

---

## timeline

### `timeline-list`

list timeline

**Arguments:**
  - `--id` (string) — timeline identifier

### `timeline-create`

create timeline

**Arguments:**
  - `--id` (string) — timeline identifier

### `timeline-get`

get timeline

**Arguments:**
  - `id` (string, required) — timeline identifier

### `timeline-update`

update timeline

**Arguments:**
  - `id` (string, required) — timeline identifier

### `timeline-delete`

delete timeline

**Arguments:**
  - `id` (string, required) — timeline identifier

---

## clip

### `clip-list`

list clip

**Arguments:**
  - `--id` (string) — clip identifier

### `clip-create`

create clip

**Arguments:**
  - `--id` (string) — clip identifier

### `clip-get`

get clip

**Arguments:**
  - `id` (string, required) — clip identifier

### `clip-update`

update clip

**Arguments:**
  - `id` (string, required) — clip identifier

### `clip-delete`

delete clip

**Arguments:**
  - `id` (string, required) — clip identifier

---

## effect

### `effect-list`

list effect

**Arguments:**
  - `--id` (string) — effect identifier

### `effect-create`

create effect

**Arguments:**
  - `--id` (string) — effect identifier

### `effect-get`

get effect

**Arguments:**
  - `id` (string, required) — effect identifier

### `effect-update`

update effect

**Arguments:**
  - `id` (string, required) — effect identifier

### `effect-delete`

delete effect

**Arguments:**
  - `id` (string, required) — effect identifier

---

## transition

### `transition-list`

list transition

**Arguments:**
  - `--id` (string) — transition identifier

### `transition-create`

create transition

**Arguments:**
  - `--id` (string) — transition identifier

### `transition-get`

get transition

**Arguments:**
  - `id` (string, required) — transition identifier

### `transition-update`

update transition

**Arguments:**
  - `id` (string, required) — transition identifier

### `transition-delete`

delete transition

**Arguments:**
  - `id` (string, required) — transition identifier

---

## render

### `render-list`

list render

**Arguments:**
  - `--id` (string) — render identifier

### `render-create`

create render

**Arguments:**
  - `--id` (string) — render identifier

### `render-get`

get render

**Arguments:**
  - `id` (string, required) — render identifier

### `render-update`

update render

**Arguments:**
  - `id` (string, required) — render identifier

### `render-delete`

delete render

**Arguments:**
  - `id` (string, required) — render identifier
