# Command Reference — cli-anything-inkscape

## document

### `document-list`

list document

**Arguments:**
  - `--id` (string) — document identifier

### `document-create`

create document

**Arguments:**
  - `--id` (string) — document identifier

### `document-get`

get document

**Arguments:**
  - `id` (string, required) — document identifier

### `document-update`

update document

**Arguments:**
  - `id` (string, required) — document identifier

### `document-delete`

delete document

**Arguments:**
  - `id` (string, required) — document identifier

---

## object

### `object-list`

list object

**Arguments:**
  - `--id` (string) — object identifier

### `object-create`

create object

**Arguments:**
  - `--id` (string) — object identifier

### `object-get`

get object

**Arguments:**
  - `id` (string, required) — object identifier

### `object-update`

update object

**Arguments:**
  - `id` (string, required) — object identifier

### `object-delete`

delete object

**Arguments:**
  - `id` (string, required) — object identifier

---

## path

### `path-list`

list path

**Arguments:**
  - `--id` (string) — path identifier

### `path-create`

create path

**Arguments:**
  - `--id` (string) — path identifier

### `path-get`

get path

**Arguments:**
  - `id` (string, required) — path identifier

### `path-update`

update path

**Arguments:**
  - `id` (string, required) — path identifier

### `path-delete`

delete path

**Arguments:**
  - `id` (string, required) — path identifier

---

## text

### `text-list`

list text

**Arguments:**
  - `--id` (string) — text identifier

### `text-create`

create text

**Arguments:**
  - `--id` (string) — text identifier

### `text-get`

get text

**Arguments:**
  - `id` (string, required) — text identifier

### `text-update`

update text

**Arguments:**
  - `id` (string, required) — text identifier

### `text-delete`

delete text

**Arguments:**
  - `id` (string, required) — text identifier

---

## export

### `export-list`

list export

**Arguments:**
  - `--id` (string) — export identifier

### `export-create`

create export

**Arguments:**
  - `--id` (string) — export identifier

### `export-get`

get export

**Arguments:**
  - `id` (string, required) — export identifier

### `export-update`

update export

**Arguments:**
  - `id` (string, required) — export identifier

### `export-delete`

delete export

**Arguments:**
  - `id` (string, required) — export identifier

---

## transform

### `transform-list`

list transform

**Arguments:**
  - `--id` (string) — transform identifier

### `transform-create`

create transform

**Arguments:**
  - `--id` (string) — transform identifier

### `transform-get`

get transform

**Arguments:**
  - `id` (string, required) — transform identifier

### `transform-update`

update transform

**Arguments:**
  - `id` (string, required) — transform identifier

### `transform-delete`

delete transform

**Arguments:**
  - `id` (string, required) — transform identifier
