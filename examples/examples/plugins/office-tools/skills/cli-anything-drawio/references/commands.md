# Command Reference — cli-anything-drawio

## diagram

### `diagram-list`

list diagram

**Arguments:**
  - `--id` (string) — diagram identifier

### `diagram-create`

create diagram

**Arguments:**
  - `--id` (string) — diagram identifier

### `diagram-get`

get diagram

**Arguments:**
  - `id` (string, required) — diagram identifier

### `diagram-update`

update diagram

**Arguments:**
  - `id` (string, required) — diagram identifier

### `diagram-delete`

delete diagram

**Arguments:**
  - `id` (string, required) — diagram identifier

---

## shape

### `shape-list`

list shape

**Arguments:**
  - `--id` (string) — shape identifier

### `shape-create`

create shape

**Arguments:**
  - `--id` (string) — shape identifier

### `shape-get`

get shape

**Arguments:**
  - `id` (string, required) — shape identifier

### `shape-update`

update shape

**Arguments:**
  - `id` (string, required) — shape identifier

### `shape-delete`

delete shape

**Arguments:**
  - `id` (string, required) — shape identifier

---

## connection

### `connection-list`

list connection

**Arguments:**
  - `--id` (string) — connection identifier

### `connection-create`

create connection

**Arguments:**
  - `--id` (string) — connection identifier

### `connection-get`

get connection

**Arguments:**
  - `id` (string, required) — connection identifier

### `connection-update`

update connection

**Arguments:**
  - `id` (string, required) — connection identifier

### `connection-delete`

delete connection

**Arguments:**
  - `id` (string, required) — connection identifier

---

## export

### `export-list`

list export

**Arguments:**
  - `--id` (string) — export identifier

### `export-create`

create export

**Arguments:**
  - `--id` (string) — export identifier

### `export-get`

get export

**Arguments:**
  - `id` (string, required) — export identifier

### `export-update`

update export

**Arguments:**
  - `id` (string, required) — export identifier

### `export-delete`

delete export

**Arguments:**
  - `id` (string, required) — export identifier

---

## style

### `style-list`

list style

**Arguments:**
  - `--id` (string) — style identifier

### `style-create`

create style

**Arguments:**
  - `--id` (string) — style identifier

### `style-get`

get style

**Arguments:**
  - `id` (string, required) — style identifier

### `style-update`

update style

**Arguments:**
  - `id` (string, required) — style identifier

### `style-delete`

delete style

**Arguments:**
  - `id` (string, required) — style identifier
