# Command Reference — cli-anything-obs-studio

## scene

### `scene-list`

list scene

**Arguments:**
  - `--id` (string) — scene identifier

### `scene-create`

create scene

**Arguments:**
  - `--id` (string) — scene identifier

### `scene-get`

get scene

**Arguments:**
  - `id` (string, required) — scene identifier

### `scene-update`

update scene

**Arguments:**
  - `id` (string, required) — scene identifier

### `scene-delete`

delete scene

**Arguments:**
  - `id` (string, required) — scene identifier

---

## source

### `source-list`

list source

**Arguments:**
  - `--id` (string) — source identifier

### `source-create`

create source

**Arguments:**
  - `--id` (string) — source identifier

### `source-get`

get source

**Arguments:**
  - `id` (string, required) — source identifier

### `source-update`

update source

**Arguments:**
  - `id` (string, required) — source identifier

### `source-delete`

delete source

**Arguments:**
  - `id` (string, required) — source identifier

---

## stream

### `stream-list`

list stream

**Arguments:**
  - `--id` (string) — stream identifier

### `stream-create`

create stream

**Arguments:**
  - `--id` (string) — stream identifier

### `stream-get`

get stream

**Arguments:**
  - `id` (string, required) — stream identifier

### `stream-update`

update stream

**Arguments:**
  - `id` (string, required) — stream identifier

### `stream-delete`

delete stream

**Arguments:**
  - `id` (string, required) — stream identifier

---

## record

### `record-list`

list record

**Arguments:**
  - `--id` (string) — record identifier

### `record-create`

create record

**Arguments:**
  - `--id` (string) — record identifier

### `record-get`

get record

**Arguments:**
  - `id` (string, required) — record identifier

### `record-update`

update record

**Arguments:**
  - `id` (string, required) — record identifier

### `record-delete`

delete record

**Arguments:**
  - `id` (string, required) — record identifier

---

## filter

### `filter-list`

list filter

**Arguments:**
  - `--id` (string) — filter identifier

### `filter-create`

create filter

**Arguments:**
  - `--id` (string) — filter identifier

### `filter-get`

get filter

**Arguments:**
  - `id` (string, required) — filter identifier

### `filter-update`

update filter

**Arguments:**
  - `id` (string, required) — filter identifier

### `filter-delete`

delete filter

**Arguments:**
  - `id` (string, required) — filter identifier

---

## transition

### `transition-list`

list transition

**Arguments:**
  - `--id` (string) — transition identifier

### `transition-create`

create transition

**Arguments:**
  - `--id` (string) — transition identifier

### `transition-get`

get transition

**Arguments:**
  - `id` (string, required) — transition identifier

### `transition-update`

update transition

**Arguments:**
  - `id` (string, required) — transition identifier

### `transition-delete`

delete transition

**Arguments:**
  - `id` (string, required) — transition identifier
