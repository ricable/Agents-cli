# Command Reference — cli-anything-shotcut

## project

### `project-list`

list project

**Arguments:**
  - `--id` (string) — project identifier

### `project-create`

create project

**Arguments:**
  - `--id` (string) — project identifier

### `project-get`

get project

**Arguments:**
  - `id` (string, required) — project identifier

### `project-update`

update project

**Arguments:**
  - `id` (string, required) — project identifier

### `project-delete`

delete project

**Arguments:**
  - `id` (string, required) — project identifier

---

## timeline

### `timeline-list`

list timeline

**Arguments:**
  - `--id` (string) — timeline identifier

### `timeline-create`

create timeline

**Arguments:**
  - `--id` (string) — timeline identifier

### `timeline-get`

get timeline

**Arguments:**
  - `id` (string, required) — timeline identifier

### `timeline-update`

update timeline

**Arguments:**
  - `id` (string, required) — timeline identifier

### `timeline-delete`

delete timeline

**Arguments:**
  - `id` (string, required) — timeline identifier

---

## clip

### `clip-list`

list clip

**Arguments:**
  - `--id` (string) — clip identifier

### `clip-create`

create clip

**Arguments:**
  - `--id` (string) — clip identifier

### `clip-get`

get clip

**Arguments:**
  - `id` (string, required) — clip identifier

### `clip-update`

update clip

**Arguments:**
  - `id` (string, required) — clip identifier

### `clip-delete`

delete clip

**Arguments:**
  - `id` (string, required) — clip identifier

---

## filter

### `filter-list`

list filter

**Arguments:**
  - `--id` (string) — filter identifier

### `filter-create`

create filter

**Arguments:**
  - `--id` (string) — filter identifier

### `filter-get`

get filter

**Arguments:**
  - `id` (string, required) — filter identifier

### `filter-update`

update filter

**Arguments:**
  - `id` (string, required) — filter identifier

### `filter-delete`

delete filter

**Arguments:**
  - `id` (string, required) — filter identifier

---

## export

### `export-list`

list export

**Arguments:**
  - `--id` (string) — export identifier

### `export-create`

create export

**Arguments:**
  - `--id` (string) — export identifier

### `export-get`

get export

**Arguments:**
  - `id` (string, required) — export identifier

### `export-update`

update export

**Arguments:**
  - `id` (string, required) — export identifier

### `export-delete`

delete export

**Arguments:**
  - `id` (string, required) — export identifier
