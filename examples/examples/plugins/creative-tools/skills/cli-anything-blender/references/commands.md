# Command Reference — cli-anything-blender

## scene

### `scene-list`

list scene

**Arguments:**
  - `--id` (string) — scene identifier

### `scene-create`

create scene

**Arguments:**
  - `--id` (string) — scene identifier

### `scene-get`

get scene

**Arguments:**
  - `id` (string, required) — scene identifier

### `scene-update`

update scene

**Arguments:**
  - `id` (string, required) — scene identifier

### `scene-delete`

delete scene

**Arguments:**
  - `id` (string, required) — scene identifier

---

## object

### `object-list`

list object

**Arguments:**
  - `--id` (string) — object identifier

### `object-create`

create object

**Arguments:**
  - `--id` (string) — object identifier

### `object-get`

get object

**Arguments:**
  - `id` (string, required) — object identifier

### `object-update`

update object

**Arguments:**
  - `id` (string, required) — object identifier

### `object-delete`

delete object

**Arguments:**
  - `id` (string, required) — object identifier

---

## mesh

### `mesh-list`

list mesh

**Arguments:**
  - `--id` (string) — mesh identifier

### `mesh-create`

create mesh

**Arguments:**
  - `--id` (string) — mesh identifier

### `mesh-get`

get mesh

**Arguments:**
  - `id` (string, required) — mesh identifier

### `mesh-update`

update mesh

**Arguments:**
  - `id` (string, required) — mesh identifier

### `mesh-delete`

delete mesh

**Arguments:**
  - `id` (string, required) — mesh identifier

---

## material

### `material-list`

list material

**Arguments:**
  - `--id` (string) — material identifier

### `material-create`

create material

**Arguments:**
  - `--id` (string) — material identifier

### `material-get`

get material

**Arguments:**
  - `id` (string, required) — material identifier

### `material-update`

update material

**Arguments:**
  - `id` (string, required) — material identifier

### `material-delete`

delete material

**Arguments:**
  - `id` (string, required) — material identifier

---

## render

### `render-list`

list render

**Arguments:**
  - `--id` (string) — render identifier

### `render-create`

create render

**Arguments:**
  - `--id` (string) — render identifier

### `render-get`

get render

**Arguments:**
  - `id` (string, required) — render identifier

### `render-update`

update render

**Arguments:**
  - `id` (string, required) — render identifier

### `render-delete`

delete render

**Arguments:**
  - `id` (string, required) — render identifier

---

## animation

### `animation-list`

list animation

**Arguments:**
  - `--id` (string) — animation identifier

### `animation-create`

create animation

**Arguments:**
  - `--id` (string) — animation identifier

### `animation-get`

get animation

**Arguments:**
  - `id` (string, required) — animation identifier

### `animation-update`

update animation

**Arguments:**
  - `id` (string, required) — animation identifier

### `animation-delete`

delete animation

**Arguments:**
  - `id` (string, required) — animation identifier

---

## modifier

### `modifier-list`

list modifier

**Arguments:**
  - `--id` (string) — modifier identifier

### `modifier-create`

create modifier

**Arguments:**
  - `--id` (string) — modifier identifier

### `modifier-get`

get modifier

**Arguments:**
  - `id` (string, required) — modifier identifier

### `modifier-update`

update modifier

**Arguments:**
  - `id` (string, required) — modifier identifier

### `modifier-delete`

delete modifier

**Arguments:**
  - `id` (string, required) — modifier identifier

---

## export

### `export-list`

list export

**Arguments:**
  - `--id` (string) — export identifier

### `export-create`

create export

**Arguments:**
  - `--id` (string) — export identifier

### `export-get`

get export

**Arguments:**
  - `id` (string, required) — export identifier

### `export-update`

update export

**Arguments:**
  - `id` (string, required) — export identifier

### `export-delete`

delete export

**Arguments:**
  - `id` (string, required) — export identifier
