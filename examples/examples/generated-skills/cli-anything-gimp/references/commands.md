# Command Reference — cli-anything-gimp

## project

### `project-list`

list project

**Arguments:**
  - `--id` (string) — project identifier

### `project-create`

create project

**Arguments:**
  - `--id` (string) — project identifier

### `project-get`

get project

**Arguments:**
  - `id` (string, required) — project identifier

### `project-update`

update project

**Arguments:**
  - `id` (string, required) — project identifier

### `project-delete`

delete project

**Arguments:**
  - `id` (string, required) — project identifier

---

## image

### `image-list`

list image

**Arguments:**
  - `--id` (string) — image identifier

### `image-create`

create image

**Arguments:**
  - `--id` (string) — image identifier

### `image-get`

get image

**Arguments:**
  - `id` (string, required) — image identifier

### `image-update`

update image

**Arguments:**
  - `id` (string, required) — image identifier

### `image-delete`

delete image

**Arguments:**
  - `id` (string, required) — image identifier

---

## layer

### `layer-list`

list layer

**Arguments:**
  - `--id` (string) — layer identifier

### `layer-create`

create layer

**Arguments:**
  - `--id` (string) — layer identifier

### `layer-get`

get layer

**Arguments:**
  - `id` (string, required) — layer identifier

### `layer-update`

update layer

**Arguments:**
  - `id` (string, required) — layer identifier

### `layer-delete`

delete layer

**Arguments:**
  - `id` (string, required) — layer identifier

---

## filter

### `filter-list`

list filter

**Arguments:**
  - `--id` (string) — filter identifier

### `filter-create`

create filter

**Arguments:**
  - `--id` (string) — filter identifier

### `filter-get`

get filter

**Arguments:**
  - `id` (string, required) — filter identifier

### `filter-update`

update filter

**Arguments:**
  - `id` (string, required) — filter identifier

### `filter-delete`

delete filter

**Arguments:**
  - `id` (string, required) — filter identifier

---

## color

### `color-list`

list color

**Arguments:**
  - `--id` (string) — color identifier

### `color-create`

create color

**Arguments:**
  - `--id` (string) — color identifier

### `color-get`

get color

**Arguments:**
  - `id` (string, required) — color identifier

### `color-update`

update color

**Arguments:**
  - `id` (string, required) — color identifier

### `color-delete`

delete color

**Arguments:**
  - `id` (string, required) — color identifier

---

## batch

### `batch-list`

list batch

**Arguments:**
  - `--id` (string) — batch identifier

### `batch-create`

create batch

**Arguments:**
  - `--id` (string) — batch identifier

### `batch-get`

get batch

**Arguments:**
  - `id` (string, required) — batch identifier

### `batch-update`

update batch

**Arguments:**
  - `id` (string, required) — batch identifier

### `batch-delete`

delete batch

**Arguments:**
  - `id` (string, required) — batch identifier

---

## export

### `export-list`

list export

**Arguments:**
  - `--id` (string) — export identifier

### `export-create`

create export

**Arguments:**
  - `--id` (string) — export identifier

### `export-get`

get export

**Arguments:**
  - `id` (string, required) — export identifier

### `export-update`

update export

**Arguments:**
  - `id` (string, required) — export identifier

### `export-delete`

delete export

**Arguments:**
  - `id` (string, required) — export identifier
